#include "main.h"

/**
 * swap_int - swaps the values of two integers
 * the pointers point to 98 and 42
 * @a: pointer to the variable swap
 * @b: pointer to the variable to swap
 */

void swap_int(int *a, int *b)
{
	*a = 42;
	*b = 98;
}
